<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <br>Nombre de la empresa</br>
    <%= f.text_field :t_name %>
  </div>
  <div class="field">
    <br>Cargar direccion del logo</br>
    <%= f.text_field :t_logo %>
  </div>
  <div class="field">
    <br>Eslogar de la empresa</br>
    <%= f.text_field :t_slogan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
